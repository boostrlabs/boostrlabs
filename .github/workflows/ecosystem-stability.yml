name: BOOSTR Ecosystem Stability

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'functions/**'
      - 'public/**'
      - 'scripts/ecosystem-stability.mjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'vite.config.js'
      - '.github/workflows/ecosystem-stability.yml'
  pull_request:
    paths:
      - 'functions/**'
      - 'public/**'
      - 'scripts/ecosystem-stability.mjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'vite.config.js'
      - '.github/workflows/ecosystem-stability.yml'

permissions:
  contents: read

jobs:
  stability:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --reporter=silent

      - name: Run ecosystem stability checks
        run: node scripts/ecosystem-stability.mjs

      - name: Build production bundle
        run: pnpm run build
