name: BOOSTR PWA Health

on:
  push:
    branches:
      - pwa-pilot
    paths:
      - 'index.html'
      - 'public/accept-invite/index.html'
      - 'public/manifest.webmanifest'
      - 'public/service-worker.js'
      - 'public/pwa-register.js'
      - 'public/pwa.css'
      - 'public/offline.html'
      - 'public/_headers'
      - 'scripts/pwa-health.mjs'
      - 'vite.config.js'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/pwa-health.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'public/accept-invite/index.html'
      - 'public/manifest.webmanifest'
      - 'public/service-worker.js'
      - 'public/pwa-register.js'
      - 'public/pwa.css'
      - 'public/offline.html'
      - 'public/_headers'
      - 'scripts/pwa-health.mjs'
      - 'vite.config.js'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/pwa-health.yml'

permissions:
  contents: read

concurrency:
  group: boostr-pwa-health-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run health:pwa
      - run: pnpm run stability:check
      - run: pnpm run build
      - name: Verify built PWA assets and entry flows
        run: |
          test -f dist/manifest.webmanifest
          test -f dist/service-worker.js
          test -f dist/pwa-register.js
          test -f dist/pwa.css
          test -f dist/offline.html
          test -f dist/accept-invite/index.html
          grep -q '/manifest.webmanifest' dist/index.html
          grep -q '/pwa-register.js' dist/index.html
          grep -q 'apple-mobile-web-app-capable' dist/index.html
          grep -q 'viewport-fit=cover' dist/index.html
          grep -q 'Construimos el sistema operativo de tu negocio.' dist/index.html
          grep -q 'Descubre qué necesita tu negocio.' dist/index.html
          grep -q '¿Ya tienes acceso?' dist/index.html
          grep -q 'href="/audit/"' dist/index.html
          grep -q 'href="/login/"' dist/index.html
          grep -q 'href="/accept-invite/"' dist/index.html
          grep -q 'Tu espacio BOOSTR está listo.' dist/accept-invite/index.html
          grep -q 'No necesitas ingresar información de pago.' dist/accept-invite/index.html
