# BOOSTR Labs v0.2.5 Front-End Network Repair Patch

Purpose: make the ecosystem feel connected before Codex/backend work begins.

## Adds / replaces
- `/` and `/home`: connected BOOSTR Network Home
- `/ecosystem`: operating-system map
- `/modules`: QA/module registry
- `/login`: role-based login shell
- `/manager` and `/manager-os`: BOOSTR Manager OS shell
- `/app`, `/client`, `/client-os`: Client OS hub
- `/admin`: technical Admin OS shell
- `/partner-dashboard` and `/partner-os`: partner dashboard shell
- `/partner`, `/partner/gemese`, `/partner/janko`, `/partner/omgbeauty`: connected public partner pages
- `/portfolio`, `/portfolio/omgbeauty`, `/omgbeauty`: portfolio network pages

## Does not implement backend
Auth, permissions, data, payments, order history, CRM, referrals and notifications remain pending for Codex/backend.

## Deploy
Unzip over local repo, commit, push.

Suggested commit:
`Connect BOOSTR frontend network routes`
