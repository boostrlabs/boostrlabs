# NNE web assets

Place optimized launch exports in these folders using the exact filenames from `ASSET_SHOPPING_LIST.md`:

- `brand/`
- `artists/`
- `music/`
- `quests/`
- `rewards/`
- `season/`

The UI keeps a styled fallback when a file has not arrived yet, so missing assets never render as broken images.
